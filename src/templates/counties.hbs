   {{#each this}}
            <li class="list-group-item">
                <img src="{{this.flags.svg}}" alt="{{this.name}}" width="100" height="100">
                <span class="card-title"><b> {{name}}</b></span>
            </li>
            {{/each}}